package msharp.NotePopulation;

public class IntByReference {
    // since we need to pass a int by reference in NodePopulation, we need this class.
    public int value;
    
    public IntByReference (int value)
    {
        this.value = value;
    }
}
